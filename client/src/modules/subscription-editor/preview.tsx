import { useFormikContext } from "formik";
import type { EmbedFormInitialValues } from "../../types/types";

export default function EmbedPreview() {
    const formik = useFormikContext<EmbedFormInitialValues>();
    return (
        <>
        </>
    );
}